# Report with dead citation

Agent claimed support for a new API at [Missing asyncio guide](https://example.invalid/dead-citation-404).

Also references a host that never answers: [Network fail resource](https://example.invalid/network-fail).
